apiVersion: apps/v1
kind: Deployment
metadata:
  name: flexo-collector
  labels:
    app: flexo-collector
spec:
  selector:
    matchLabels:
      app: flexo-collector
  template:
    metadata:
      labels:
        app: flexo-collector
    spec:
      containers:
      - name: flexo-collector
        image: flexo-collector
        args:
          - --config=/etc/otel/collector-config.yaml
        env:
          - name: DISCORD_TOKEN
            valueFrom:
              secretKeyRef:
                name: discord-token
                key: token
        volumeMounts:
          - name: config
            mountPath: /etc/otel
        ports:
        - containerPort: 4317
        - containerPort: 4318
      volumes:
        - name: config
          configMap:
            name: flexo-collector-config

---
apiVersion: v1
kind: Secret
metadata:
  name: discord-token
type: Opaque
stringData:
  token: "YOUR_DISCORD_TOKEN_HERE"  # Replace with your actual Discord token
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: flexo-collector-config
data:
  collector-config.yaml: |
    receivers:
      otlp:
        protocols:
          grpc:
            endpoint: 0.0.0.0:4317
          http:
            endpoint: 0.0.0.0:4318
      discord:
        token: ${DISCORD_TOKEN}

    processors:
      batch:

    exporters:
      debug:
        verbosity: detailed

    service:
      pipelines:
        traces:
          receivers: [otlp]
          processors: [batch]
          exporters: [debug]
        metrics:
          receivers: [otlp]
          processors: [batch]
          exporters: [debug]
        logs:
          receivers: [discord]
          processors: [batch]
          exporters: [debug]
